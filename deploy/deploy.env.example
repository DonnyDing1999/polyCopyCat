# 复制为同目录的 deploy.env，填入你自己的值。deploy.env 已在 .gitignore，不会入库。
# systemd 服务用 EnvironmentFile 读它。

# Discord 频道 webhook（频道设置 → 整合 → Webhook → 新建 → 复制 URL）。
# 半机密：拿到就能往你频道发消息，所以只放这里、绝不进配置文件或仓库。
POLYCOPYCAT_DISCORD_WEBHOOK=https://discord.com/api/webhooks/xxxxxxxx/yyyyyyyy
